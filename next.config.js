/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['public'], // Replace with the domain name of external images
    },
  };
  
  module.exports = nextConfig;
  